apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: skills-network-learner
  resources:
    requests:
      storage: 1Gi
